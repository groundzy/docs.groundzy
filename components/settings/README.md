# Settings Components

Settings-related components in `components/settings/`.

## Components

| Component | Purpose |
|-----------|---------|
| address-autocomplete.tsx | Mapbox address autocomplete |

Uses `NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN` for geocoding.
