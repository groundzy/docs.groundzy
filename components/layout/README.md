# Layout Components

Layout components in `components/layout/`.

## Components

| Component | Purpose |
|-----------|---------|
| app-layout.tsx | Main app layout (sidebar + map + work area) |

Orchestrates sidebar, map, and work area for desktop and mobile.
